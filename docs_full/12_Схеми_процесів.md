# 12. Схеми процесів

> Актуально: 2026-07. Стандарт компактності (для екрана телефона):
> init-директива нижче + короткі підписи з `<br/>` + орієнтація TD +
> великі процеси порізані на кілька малих діаграм.
> Директиву копіювати ПЕРШИМ рядком у кожну нову діаграму:
> `%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%`

## 1. Старт застосунку
```mermaid
%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%
flowchart TD
    A[__main__] --> B[Ранній логер<br/>імпортів]
    B -->|OK| C[MainApp.build]
    B -->|FAIL| Z[import.log +<br/>аварійний екран]
    C --> D[Lang + Config]
    C --> E[KV: екрани +<br/>drawer + preloader]
    E --> F[portrait · immersive ·<br/>інсети · патч модалок]
    F --> G[Clock +0.5с<br/>_init_db]
```
```mermaid
%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%
flowchart TD
    G[creator.initialize] --> H{getExternal<br/>FilesDir?}
    H -->|так| I[Molfar_System<br/>зовнішня тека]
    H -->|ні| J[user_data_dir<br/>fallback]
    I --> K[Підтеки + 6 БД<br/>версії/міграції + сиди]
    J --> K
    K --> L[Менеджери на app:<br/>db·chat·ai·part·<br/>mem·roles·archivist]
    L --> M[Preloader ≥1.2с<br/>→ home]
    K -->|збій| Z2[Діалог помилки БД<br/>обмежений режим]
```

## 2. Раунд наради — фаза 1 (паралель)
```mermaid
%%{init: {"themeVariables":{"fontSize":"10px"}}}%%
sequenceDiagram
    autonumber
    actor U as Юзер
    participant S as Екран
    participant A as APIMgr
    participant P as Слоти 1–4
    U->>S: питання
    S->>S: token++<br/>перевірка слотів
    par кожен слот
        S->>A: system=роль+скіл+<br/>docs(гібрид) · історія 25
        A->>P: adapter.call
        P-->>A: text / err / timeout
        A-->>S: 1 колбек (Clock)
        S->>S: insert у таб
    end
    S->>S: _pending==0 →<br/>оркестратор
```

## 3. Раунд наради — фаза 2 (Оркестратор + черга)
```mermaid
%%{init: {"themeVariables":{"fontSize":"10px"}}}%%
sequenceDiagram
    autonumber
    participant S as Екран
    participant M as memory.db
    participant O as Оркестратор
    actor U as Юзер
    S->>M: pinned + search(питання)
    S->>O: orch_composite +<br/>памʼять + відповіді 1–4
    O-->>S: синтез + черга<br/>«N. Роль — питання?»
    S->>S: insert таб5 + спільний чат<br/>+ збереження суті в память
    S-->>U: кнопка «Черга (N)»
    U->>S: правки → старт
    loop кожне питання
        S->>O: адресний промпт → слот
    end
    S->>O: синтез стенограми р.2
    O-->>U: підсумок
```

## 4. Секретар (слот 6)
```mermaid
%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%
flowchart TD
    Q[Питання] --> K[keywords<br/>casefold]
    K --> S[SQL: text_norm<br/>LIKE + scope]
    S --> R[ранжування<br/>TOP-5]
    P[(knowledge.db)] --> S
    P --> O[огляди всіх<br/>документів ord=0]
    R --> B[archivist_place:<br/>список+огляди+фрагменти]
    O --> B
    B --> A[модель слота 6<br/>відповідь з цитуванням]
```
Протокол: підсумки таба 5 + метадані + склад → archivist_protocol →
Exports + Download.

## 5. Гайдук: повідомлення з гібридною подачею
```mermaid
%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%
flowchart TD
    T[send_message] --> S[system = база +<br/>роль + документи]
    S --> L[перелік файлів<br/>ЗАВЖДИ]
    S --> F[малі ≤50К повністю<br/>бюджет 120К]
    S --> G[великі: огляди +<br/>TOP-8 фрагментів]
    G --> H{чанків нема?}
    H -->|так| I[фонова<br/>переіндексація]
    S --> A[APIManager] --> DB[(haiduk_messages)]
```

## 6. Додавання документів (канон потоків!) 
Позначки: `[J]`=Java-потік, `[B]`=фоновий, `[K]`=Kivy.
```mermaid
%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%
flowchart TD
    A["[K] Додати<br/>busy-гейт"] --> B["[K] SAF intent<br/>MIME-фільтр"]
    B --> C["[J] on_activity_result<br/>код? unbind свого"]
    C --> D["[B] копія content://<br/>→ локальні файли<br/>detach jni"]
    D --> E["[K] Clock:<br/>on_picked(список)"]
    E --> F["[B] екстракція+<br/>валідація файлів"]
    F --> G["[K] Clock: фіналізація<br/>ліміти·заміна·БД"]
    G --> H["[B] індексація<br/>knowledge.db"]
    G --> I["[K] список на екрані<br/>+ звіт"]
    H --> J["[K] снекбар<br/>N/N"]
```

## 7. Потік AI-запиту (спільний)
```mermaid
%%{init: {"themeVariables":{"fontSize":"11px"},"flowchart":{"nodeSpacing":18,"rankSpacing":22,"padding":4}}}%%
flowchart TD
    A[ask_with_config] --> W[watchdog<br/>timeout+10с]
    A --> T[daemon-потік]
    T --> G{get_adapter}
    G -->|None| E1[err_provider]
    G --> C[adapter.call<br/>google/nvidia/or]
    C -->|OK| F[_finish True]
    C -->|AdapterError| F2[_finish False<br/>локалізовано]
    W -->|завис| F3[_finish timeout]
    F & F2 & F3 --> D[settled: РІВНО 1<br/>колбек через Clock]
```

## 8. Залежності модулів (спрощено)
```mermaid
%%{init: {"themeVariables":{"fontSize":"10px"},"flowchart":{"nodeSpacing":14,"rankSpacing":18,"padding":3}}}%%
flowchart TD
    M[main] --> SC[screens] & POP[pop_ai] & PCS[project_chat] & CRE[creator]
    CRE --> SCH[schemas] & SEED[roles_seed]
    POP & PCS --> API[manage_api] --> AD[ai_adapters]
    PCS --> MD[meeting_docs] & MQ[meeting_queue] & ENG[engine]
    POP --> ENG
    MD --> ENG
    ENG --> ADB[archivist_db]
    MD & POP --> DP[doc_parser]
    SC --> MODALS[*_modal]
    MODALS & PCS & POP --> MGRS[(6 менеджерів БД)]
    MGRS --> SCH
```
